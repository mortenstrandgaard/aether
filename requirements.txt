# AETHER Analyzer v1.0 — No-AI Classical Audio Dissection Tool
# Python 3.11+ recommended

streamlit>=1.32.0
librosa>=0.10.1
numpy>=1.24.0
scipy>=1.11.0
soundfile>=0.12.1
plotly>=5.18.0
matplotlib>=3.8.0
mido>=1.3.0
yt-dlp>=2024.1.0
scikit-learn>=1.3.0
pandas>=2.0.0

# Optional (better key detection) — install separately if available:
#   pip install essentia
#
# Optional (extra onset backend) — install separately if available:
#   pip install aubio
